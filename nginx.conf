# Generated by nginxconfig.io
# See nginxconfig.txt for the configuration share link
# generated 2023-12-28, Mozilla Guideline v5.7, nginx 1.17.7, OpenSSL 1.1.1k, intermediate configuration
# https://ssl-config.mozilla.org/#server=nginx&version=1.17.7&config=intermediate&openssl=1.1.1k&guideline=5.7
# https://ssl-config.mozilla.org/

worker_processes        auto;

events {
    worker_connections  1024;
}

http {
    charset                     utf-8;
    sendfile                    on;
    tcp_nopush                  on;
    tcp_nodelay                 on;
    server_tokens               off;
    log_not_found               off;
    types_hash_max_size         2048;
    types_hash_bucket_size      128;
    client_max_body_size        16M;
    keepalive_timeout           60;

    # MIME
    include                     mime.types;
    default_type                application/octet-stream;

    # XStack Https Server
    server {
        listen                  443 ssl;
        listen                  [::]:443 ssl;
        server_name             www.libsx.com;

        # SSL
        ssl_certificate         /etc/nginx/pem/fullchain.pem;
        ssl_certificate_key     /etc/nginx/pem/certkey.pem;
        ssl_trusted_certificate /etc/nginx/pem/cert.pem;

        # ssl_session_timeout 1d;
        # ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions
        # ssl_session_tickets off;

        # intermediate configuration
        # ssl_protocols TLSv1.2 TLSv1.3;
        # ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:DHE-RSA-CHACHA20-POLY1305;
        # ssl_prefer_server_ciphers off;

        # logging
        access_log              /var/log/nginx/access.log combined buffer=512k flush=1m;
        error_log               /var/log/nginx/error.log warn;

        # gzip
        # gzip                    on;
        # gzip_vary               on;
        # gzip_proxied            any;
        # gzip_comp_level         6;
        # gzip_types              text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

        # reverse proxy
        location / {
            proxy_pass          http://8.141.90.147:3000;
            proxy_set_header    Host $host;
        }

        # additional config
        # include nginxconfig.io/general.conf;
    }

    # XStack Http Server
    server {
        listen                  80;
        server_name             libsx.com *.libsx.com;

        location / {
            # return 301 https://www.libsx.com$request_uri;
            proxy_pass          http://8.141.90.147:3000;
            proxy_set_header    Host $host;
            proxy_set_header    X-Real-IP $remote_addr;
            proxy_set_header    X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header    X-Forwarded-Proto $scheme;
        }
    }
}